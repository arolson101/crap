import { StyleSheet } from 'react-native';

export const formStyles = StyleSheet.create({
  picker: {
    flex: 1,
    borderWidth: 0,
  },
  pickerItem: {
    justifyContent: 'flex-end',
  },
  errorSubtitle: {
    color: 'red',
  },
});
